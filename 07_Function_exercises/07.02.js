// 07.02 JavasScript code

// The function Math.random() returns a random value from the range [0, 1[. 
// (1) Call the function and assign the return value to the variable randomizedNumber. 
function myFunction () {
    var randomizedNumber = Math.random();
    console.log(randomizedNumber);
}


// (2) Write the value of the variable into the browser console. 
myFunction(1);
